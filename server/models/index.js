const User = require('./User');
const { Exercises } = require('./Exercises');
const Workout = require('./Workout')
const Template = require('./Template')

module.exports = { User, Exercises, Workout, Template };
