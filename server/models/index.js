const User = require('./User');
const { Exercises } = require('./Exercises');
const Workouts = require('./Workout')
const Template = require('./Template')

module.exports = { User, Exercises, Workouts, Template };
